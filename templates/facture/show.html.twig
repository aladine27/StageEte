{% extends 'base.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
    <h1>Facture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facture.id }}</td>
            </tr>
            <tr>
                <th>Num_Facture</th>
                <td>{{ facture.NumFacture }}</td>
            </tr>
            <tr>
                <th>Type_Fact</th>
                <td>{{ facture.TypeFact }}</td>
            </tr>
            <tr>
                <th>Date_Lim_Pay</th>
                <td>{{ facture.DateLimPay ? facture.DateLimPay|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Net_Apayer</th>
                <td>{{ facture.NetApayer }}</td>
            </tr>
            <tr>
                <th>Anc_Index</th>
                <td>{{ facture.AncIndex }}</td>
            </tr>
            <tr>
                <th>Nouv_Inedx</th>
                <td>{{ facture.NouvInedx }}</td>
            </tr>
            <tr>
                <th>Estimation</th>
                <td>{{ facture.Estimation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_facture_index') }}">back to list</a>

    <a href="{{ path('app_facture_edit', {'id': facture.id}) }}">edit</a>

    {{ include('facture/_delete_form.html.twig') }}
{% endblock %}
